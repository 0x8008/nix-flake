diff -urN qemu-orig/block/vhdx.c qemu-patched/block/vhdx.c
--- qemu-orig/block/vhdx.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/block/vhdx.c	2026-05-13 18:42:35.563559136 +0200
@@ -2020,7 +2020,7 @@
 
     /* The creator field is optional, but may be useful for
      * debugging / diagnostics */
-    creator = g_utf8_to_utf16("QEMU v" QEMU_VERSION, -1, NULL,
+    creator = g_utf8_to_utf16("SOYO v" QEMU_VERSION, -1, NULL,
                               &creator_items, NULL);
     signature = cpu_to_le64(VHDX_FILE_SIGNATURE);
     ret = blk_co_pwrite(blk, VHDX_FILE_ID_OFFSET, sizeof(signature), &signature,
diff -urN qemu-orig/block/vvfat.c qemu-patched/block/vvfat.c
--- qemu-orig/block/vvfat.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/block/vvfat.c	2026-05-13 18:42:35.563791500 +0200
@@ -1176,7 +1176,7 @@
         }
         memcpy(s->volume_label, label, label_length);
     } else {
-        memcpy(s->volume_label, "QEMU VVFAT", 10);
+        memcpy(s->volume_label, "SOYO VVFAT", 10);
     }
 
     if (floppy) {
diff -urN qemu-orig/chardev/msmouse.c qemu-patched/chardev/msmouse.c
--- qemu-orig/chardev/msmouse.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/chardev/msmouse.c	2026-05-13 18:42:35.563956352 +0200
@@ -172,7 +172,7 @@
 }
 
 static const QemuInputHandler msmouse_handler = {
-    .name  = "QEMU Microsoft Mouse",
+    .name  = "SOYO Microsoft Mouse",
     .mask  = INPUT_EVENT_MASK_BTN | INPUT_EVENT_MASK_REL,
     .event = msmouse_input_event,
     .sync  = msmouse_input_sync,
diff -urN qemu-orig/chardev/wctablet.c qemu-patched/chardev/wctablet.c
--- qemu-orig/chardev/wctablet.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/chardev/wctablet.c	2026-05-13 18:42:35.564006470 +0200
@@ -179,7 +179,7 @@
 }
 
 static const QemuInputHandler wctablet_handler = {
-    .name  = "QEMU Wacom Pen Tablet",
+    .name  = "SOYO Wacom Pen Tablet",
     .mask  = INPUT_EVENT_MASK_BTN | INPUT_EVENT_MASK_ABS,
     .event = wctablet_input_event,
     .sync  = wctablet_input_sync,
diff -urN qemu-orig/contrib/vhost-user-gpu/vhost-user-gpu.c qemu-patched/contrib/vhost-user-gpu/vhost-user-gpu.c
--- qemu-orig/contrib/vhost-user-gpu/vhost-user-gpu.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/contrib/vhost-user-gpu/vhost-user-gpu.c	2026-05-13 18:42:35.564089833 +0200
@@ -1254,7 +1254,7 @@
     QTAILQ_INIT(&g.reslist);
     QTAILQ_INIT(&g.fenceq);
 
-    context = g_option_context_new("QEMU vhost-user-gpu");
+    context = g_option_context_new("SOYO vhost-user-gpu");
     g_option_context_add_main_entries(context, entries, NULL);
     if (!g_option_context_parse(context, &argc, &argv, &error)) {
         g_printerr("Option parsing failed: %s\n", error->message);
diff -urN qemu-orig/docs/specs/fw_cfg.rst qemu-patched/docs/specs/fw_cfg.rst
--- qemu-orig/docs/specs/fw_cfg.rst	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/docs/specs/fw_cfg.rst	2026-05-13 18:42:35.564181101 +0200
@@ -97,7 +97,7 @@
 ACPI Interface
 --------------
 
-The fw_cfg device is defined with ACPI ID ``QEMU0002``. Since we expect
+The fw_cfg device is defined with ACPI ID ``SOYO0002``. Since we expect
 ACPI tables to be passed into the guest through the fw_cfg device itself,
 the guest-side firmware can not use ACPI to find fw_cfg. However, once the
 firmware is finished setting up ACPI tables and hands control over to the
diff -urN qemu-orig/hw/acpi/aml-build.c qemu-patched/hw/acpi/aml-build.c
--- qemu-orig/hw/acpi/aml-build.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/hw/acpi/aml-build.c	2026-05-13 18:42:53.090298431 +0200
@@ -1722,11 +1722,11 @@
     build_append_int_noprefix(array, 0, 4); /* Length */
     build_append_int_noprefix(array, desc->rev, 1); /* Revision */
     build_append_int_noprefix(array, 0, 1); /* Checksum */
-    build_append_padded_str(array, desc->oem_id, 6, '\0'); /* OEMID */
+    build_append_padded_str(array, ACPI_BUILD_APPNAME6, 6, '\0'); /* OEMID */
     /* OEM Table ID */
-    build_append_padded_str(array, desc->oem_table_id, 8, '\0');
+    build_append_padded_str(array, ACPI_BUILD_APPNAME8, 8, '\0');
     build_append_int_noprefix(array, 1, 4); /* OEM Revision */
-    g_array_append_vals(array, ACPI_BUILD_APPNAME8, 4); /* Creator ID */
+    g_array_append_vals(array, "PTL ", 4); /* Creator ID */
     build_append_int_noprefix(array, 1, 4); /* Creator Revision */
 }
 
@@ -2360,7 +2360,7 @@
     }
 
     /* Hypervisor Vendor Identity */
-    build_append_padded_str(tbl, "QEMU", 8, '\0');
+    build_append_padded_str(tbl, "SOYO", 8, '\0');
 
     /* TODO: extra fields need to be added to support revisions above rev6 */
     assert(f->rev == 6);
diff -urN qemu-orig/hw/arm/sbsa-ref.c qemu-patched/hw/arm/sbsa-ref.c
--- qemu-orig/hw/arm/sbsa-ref.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/hw/arm/sbsa-ref.c	2026-05-13 18:42:35.564435498 +0200
@@ -898,7 +898,7 @@
     };
 
     mc->init = sbsa_ref_init;
-    mc->desc = "QEMU 'SBSA Reference' ARM Virtual Machine";
+    mc->desc = "SOYO 'SBSA Reference' ARM Real Machine";
     mc->default_cpu_type = ARM_CPU_TYPE_NAME("neoverse-n2");
     mc->valid_cpu_types = valid_cpu_types;
     mc->max_cpus = 512;
diff -urN qemu-orig/hw/arm/virt.c qemu-patched/hw/arm/virt.c
--- qemu-orig/hw/arm/virt.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/hw/arm/virt.c	2026-05-13 18:42:35.564630049 +0200
@@ -1797,13 +1797,13 @@
     uint8_t *smbios_tables, *smbios_anchor;
     size_t smbios_tables_len, smbios_anchor_len;
     struct smbios_phys_mem_area mem_array;
-    const char *product = "QEMU Virtual Machine";
+    const char *product = "SOYO Real Machine";
 
     if (kvm_enabled()) {
-        product = "KVM Virtual Machine";
+        product = "SOYO Real Machine";
     }
 
-    smbios_set_defaults("QEMU", product, mc->name);
+    smbios_set_defaults("SOYO", product, mc->name);
 
     /* build the array of physical mem area from base_memmap */
     mem_array.address = vms->memmap[VIRT_MEM].base;
diff -urN qemu-orig/hw/audio/hda-codec.c qemu-patched/hw/audio/hda-codec.c
--- qemu-orig/hw/audio/hda-codec.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/hw/audio/hda-codec.c	2026-05-13 18:42:35.564844147 +0200
@@ -118,7 +118,7 @@
 
 /* some defines */
 
-#define QEMU_HDA_ID_VENDOR  0x1af4
+#define QEMU_HDA_ID_VENDOR  0x8086
 #define QEMU_HDA_PCM_FORMATS (AC_SUPPCM_BITS_16 |       \
                               0x1fc /* 16 -> 96 kHz */)
 #define QEMU_HDA_AMP_NONE    (0)
diff -urN qemu-orig/hw/char/escc.c qemu-patched/hw/char/escc.c
--- qemu-orig/hw/char/escc.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/hw/char/escc.c	2026-05-13 18:42:35.564915887 +0200
@@ -1037,7 +1037,7 @@
 }
 
 static const QemuInputHandler sunmouse_handler = {
-    .name  = "QEMU Sun Mouse",
+    .name  = "SOYO Sun Mouse",
     .mask  = INPUT_EVENT_MASK_BTN | INPUT_EVENT_MASK_REL,
     .event = sunmouse_handle_event,
     .sync  = sunmouse_sync,
diff -urN qemu-orig/hw/display/edid-generate.c qemu-patched/hw/display/edid-generate.c
--- qemu-orig/hw/display/edid-generate.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/hw/display/edid-generate.c	2026-05-13 18:42:35.564987517 +0200
@@ -394,10 +394,10 @@
     /* =============== set defaults  =============== */
 
     if (!info->vendor || strlen(info->vendor) != 3) {
-        info->vendor = "RHT";
+        info->vendor = "DEL";
     }
     if (!info->name) {
-        info->name = "QEMU Monitor";
+        info->name = "DEL Monitor";
     }
     if (!info->prefx) {
         info->prefx = 1280;
@@ -449,7 +449,7 @@
     uint16_t vendor_id = ((((info->vendor[0] - '@') & 0x1f) << 10) |
                           (((info->vendor[1] - '@') & 0x1f) <<  5) |
                           (((info->vendor[2] - '@') & 0x1f) <<  0));
-    uint16_t model_nr = 0x1234;
+    uint16_t model_nr = 0xA05F;
     uint32_t serial_nr = info->serial ? atoi(info->serial) : 0;
     stw_be_p(edid +  8, vendor_id);
     stw_le_p(edid + 10, model_nr);
diff -urN qemu-orig/hw/i386/acpi-build.c qemu-patched/hw/i386/acpi-build.c
--- qemu-orig/hw/i386/acpi-build.c	2026-03-18 02:27:49.000000000 +0100
+++ qemu-patched/hw/i386/acpi-build.c	2026-05-13 18:42:35.565075369 +0200
@@ -2087,6 +2087,14 @@
         g_array_append_vals(tables_blob, u, len);
     }
 
+    /* Disable BGRT (UEFI Logo)*/
+    acpi_add_table(table_offsets, tables_blob);
+    AcpiTable table = { .sig = "BGRT", .rev = 1,
+                        .oem_id = x86ms->oem_id, .oem_table_id = x86ms->oem_table_id };
+    acpi_table_begin(&table, tables_blob);
+    build_append_int_noprefix(tables_blob,0x00000000,4);
+    acpi_table_end(tables->linker, &table);
+
     /* RSDT is pointed to by RSDP */
     rsdt = tables_blob->len;
     build_rsdt(tables_blob, tables->linker, table_offsets,
diff -urN qemu-orig/hw/i386/fw_cfg.c qemu-patched/hw/i386/fw_cfg.c
--- qemu-orig/hw/i386/fw_cfg.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/i386/fw_cfg.c	2026-05-13 18:42:35.565188209 +0200
@@ -75,7 +75,7 @@
 
     if (pcmc->smbios_defaults) {
         /* These values are guest ABI, do not change */
-        smbios_set_defaults("QEMU", mc->desc, mc->name);
+        smbios_set_defaults("SOYO", "M4A88TD-M", "SOYO-PC");
     }
 
     /* tell smbios about cpuid version and features */
@@ -228,7 +228,7 @@
     Aml *dev = aml_device("FWCF");
     Aml *crs = aml_resource_template();
 
-    aml_append(dev, aml_name_decl("_HID", aml_string("QEMU0002")));
+    aml_append(dev, aml_name_decl("_HID", aml_string("SOYO0002")));
 
     /* device present, functioning, decoding, not shown in UI */
     aml_append(dev, aml_name_decl("_STA", aml_int(0xB)));
diff -urN qemu-orig/hw/i386/pc.c qemu-patched/hw/i386/pc.c
--- qemu-orig/hw/i386/pc.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/i386/pc.c	2026-05-13 18:42:35.565259619 +0200
@@ -77,9 +77,9 @@
  * depending on QEMU versions up to QEMU 2.4.
  */
 #define PC_CPU_MODEL_IDS(v) \
-    { "qemu32-" TYPE_X86_CPU, "model-id", "QEMU Virtual CPU version " v, },\
-    { "qemu64-" TYPE_X86_CPU, "model-id", "QEMU Virtual CPU version " v, },\
-    { "athlon-" TYPE_X86_CPU, "model-id", "QEMU Virtual CPU version " v, },
+    { "SOYO32-" TYPE_X86_CPU, "model-id", "SOYO Real CPU version  v, },\
+    { "SOYO64-" TYPE_X86_CPU, "model-id", "SOYO Real CPU version  v, },\
+    { "athlon-" TYPE_X86_CPU, "model-id", "SOYO Real CPU version  v, },
 
 GlobalProperty pc_compat_10_1[] = {
     { "mch", "extended-tseg-mbytes", "16" },
diff -urN qemu-orig/hw/ide/atapi.c qemu-patched/hw/ide/atapi.c
--- qemu-orig/hw/ide/atapi.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/ide/atapi.c	2026-05-13 18:42:35.565367600 +0200
@@ -798,8 +798,8 @@
         buf[5] = 0;    /* reserved */
         buf[6] = 0;    /* reserved */
         buf[7] = 0;    /* reserved */
-        padstr8(buf + 8, 8, "QEMU");
-        padstr8(buf + 16, 16, "QEMU DVD-ROM");
+        padstr8(buf + 8, 8, "SOYO");
+        padstr8(buf + 16, 16, "SOYO DVD-ROM");
         padstr8(buf + 32, 4, s->version);
         idx = 36;
     }
diff -urN qemu-orig/hw/ide/core.c qemu-patched/hw/ide/core.c
--- qemu-orig/hw/ide/core.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/ide/core.c	2026-05-13 18:42:35.565495480 +0200
@@ -2639,20 +2639,20 @@
         pstrcpy(s->drive_serial_str, sizeof(s->drive_serial_str), dev->serial);
     } else {
         snprintf(s->drive_serial_str, sizeof(s->drive_serial_str),
-                 "QM%05d", s->drive_serial);
+                 "SOYO%05d", s->drive_serial);
     }
     if (dev->model) {
         pstrcpy(s->drive_model_str, sizeof(s->drive_model_str), dev->model);
     } else {
         switch (kind) {
         case IDE_CD:
-            strcpy(s->drive_model_str, "QEMU DVD-ROM");
+            strcpy(s->drive_model_str, "SOYO DVD-ROM");
             break;
         case IDE_CFATA:
-            strcpy(s->drive_model_str, "QEMU MICRODRIVE");
+            strcpy(s->drive_model_str, "SOYO MICRODRIVE");
             break;
         default:
-            strcpy(s->drive_model_str, "QEMU HARDDISK");
+            strcpy(s->drive_model_str, "SOYO HARDDISK");
             break;
         }
     }
diff -urN qemu-orig/hw/input/adb-kbd.c qemu-patched/hw/input/adb-kbd.c
--- qemu-orig/hw/input/adb-kbd.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/input/adb-kbd.c	2026-05-13 18:42:35.565617217 +0200
@@ -1,5 +1,5 @@
 /*
- * QEMU ADB keyboard support
+ * SOYO ADB Keyboard support
  *
  * Copyright (c) 2004 Fabrice Bellard
  *
@@ -356,7 +356,7 @@
 }
 
 static const QemuInputHandler adb_keyboard_handler = {
-    .name  = "QEMU ADB Keyboard",
+    .name  = "SOYO ADB Keyboard",
     .mask  = INPUT_EVENT_MASK_KEY,
     .event = adb_keyboard_event,
 };
diff -urN qemu-orig/hw/input/adb-mouse.c qemu-patched/hw/input/adb-mouse.c
--- qemu-orig/hw/input/adb-mouse.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/input/adb-mouse.c	2026-05-13 18:42:35.565665963 +0200
@@ -1,5 +1,5 @@
 /*
- * QEMU ADB mouse support
+ * SOYO ADB Mouse support
  *
  * Copyright (c) 2004 Fabrice Bellard
  *
@@ -94,7 +94,7 @@
 }
 
 static const QemuInputHandler adb_mouse_handler = {
-    .name  = "QEMU ADB Mouse",
+    .name  = "SOYO ADB Mouse",
     .mask  = INPUT_EVENT_MASK_BTN | INPUT_EVENT_MASK_REL,
     .event = adb_mouse_handle_event,
     /*
diff -urN qemu-orig/hw/input/hid.c qemu-patched/hw/input/hid.c
--- qemu-orig/hw/input/hid.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/input/hid.c	2026-05-13 18:42:35.565711432 +0200
@@ -512,20 +512,20 @@
 }
 
 static const QemuInputHandler hid_keyboard_handler = {
-    .name  = "QEMU HID Keyboard",
+    .name  = "SOYO HID Keyboard",
     .mask  = INPUT_EVENT_MASK_KEY,
     .event = hid_keyboard_event,
 };
 
 static const QemuInputHandler hid_mouse_handler = {
-    .name  = "QEMU HID Mouse",
+    .name  = "SOYO HID Mouse",
     .mask  = INPUT_EVENT_MASK_BTN | INPUT_EVENT_MASK_REL,
     .event = hid_pointer_event,
     .sync  = hid_pointer_sync,
 };
 
 static const QemuInputHandler hid_tablet_handler = {
-    .name  = "QEMU HID Tablet",
+    .name  = "SOYO HID Tablet",
     .mask  = INPUT_EVENT_MASK_BTN | INPUT_EVENT_MASK_ABS,
     .event = hid_pointer_event,
     .sync  = hid_pointer_sync,
diff -urN qemu-orig/hw/input/ps2.c qemu-patched/hw/input/ps2.c
--- qemu-orig/hw/input/ps2.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/input/ps2.c	2026-05-13 18:42:35.565779204 +0200
@@ -1,5 +1,5 @@
 /*
- * QEMU PS/2 keyboard/mouse emulation
+ * SOYO PS/2 Keyboard/mouse emulation
  *
  * Copyright (c) 2003 Fabrice Bellard
  *
@@ -1232,7 +1232,7 @@
 };
 
 static const QemuInputHandler ps2_keyboard_handler = {
-    .name  = "QEMU PS/2 Keyboard",
+    .name  = "SOYO PS/2 Keyboard",
     .mask  = INPUT_EVENT_MASK_KEY,
     .event = ps2_keyboard_event,
 };
@@ -1243,7 +1243,7 @@
 }
 
 static const QemuInputHandler ps2_mouse_handler = {
-    .name  = "QEMU PS/2 Mouse",
+    .name  = "SOYO PS/2 Mouse",
     .mask  = INPUT_EVENT_MASK_BTN | INPUT_EVENT_MASK_REL,
     .event = ps2_mouse_event,
     .sync  = ps2_mouse_sync,
diff -urN qemu-orig/hw/input/virtio-input-hid.c qemu-patched/hw/input/virtio-input-hid.c
--- qemu-orig/hw/input/virtio-input-hid.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/input/virtio-input-hid.c	2026-05-13 18:42:35.565854110 +0200
@@ -16,10 +16,10 @@
 
 #include "standard-headers/linux/input.h"
 
-#define VIRTIO_ID_NAME_KEYBOARD     "QEMU Virtio Keyboard"
-#define VIRTIO_ID_NAME_MOUSE        "QEMU Virtio Mouse"
-#define VIRTIO_ID_NAME_TABLET       "QEMU Virtio Tablet"
-#define VIRTIO_ID_NAME_MULTITOUCH   "QEMU Virtio MultiTouch"
+#define VIRTIO_ID_NAME_KEYBOARD     "SOYO Keyboard"
+#define VIRTIO_ID_NAME_MOUSE        "SOYO Mouse"
+#define VIRTIO_ID_NAME_TABLET       "SOYO Tablet"
+#define VIRTIO_ID_NAME_MULTITOUCH   "SOYO MultiTouch"
 
 /* ----------------------------------------------------------------- */
 
diff -urN qemu-orig/hw/loongarch/virt.c qemu-patched/hw/loongarch/virt.c
--- qemu-orig/hw/loongarch/virt.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/loongarch/virt.c	2026-05-13 18:42:35.565927524 +0200
@@ -156,7 +156,7 @@
     MachineClass *mc = MACHINE_GET_CLASS(lvms);
     uint8_t *smbios_tables, *smbios_anchor;
     size_t smbios_tables_len, smbios_anchor_len;
-    const char *product = "QEMU Virtual Machine";
+    const char *product = "SOYO Real Machine";
 
     if (!lvms->fw_cfg) {
         return;
@@ -166,7 +166,7 @@
         product = "KVM Virtual Machine";
     }
 
-    smbios_set_defaults("QEMU", product, mc->name);
+    smbios_set_defaults("SOYO", product, mc->name);
 
     smbios_get_tables(ms, SMBIOS_ENTRY_POINT_TYPE_64,
                       NULL, 0,
diff -urN qemu-orig/hw/m68k/virt.c qemu-patched/hw/m68k/virt.c
--- qemu-orig/hw/m68k/virt.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/m68k/virt.c	2026-05-13 18:42:35.566010676 +0200
@@ -313,7 +313,7 @@
 static void virt_machine_class_init(ObjectClass *oc, const void *data)
 {
     MachineClass *mc = MACHINE_CLASS(oc);
-    mc->desc = "QEMU M68K Virtual Machine";
+    mc->desc = "SOYO M68K Real Machine";
     mc->init = virt_init;
     mc->default_cpu_type = M68K_CPU_TYPE_NAME("m68040");
     mc->max_cpus = 1;
diff -urN qemu-orig/hw/nvme/ctrl.c qemu-patched/hw/nvme/ctrl.c
--- qemu-orig/hw/nvme/ctrl.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/nvme/ctrl.c	2026-05-13 18:42:35.566321413 +0200
@@ -9094,7 +9094,7 @@
 
     id->vid = cpu_to_le16(pci_get_word(pci_conf + PCI_VENDOR_ID));
     id->ssvid = cpu_to_le16(pci_get_word(pci_conf + PCI_SUBSYSTEM_VENDOR_ID));
-    strpadcpy((char *)id->mn, sizeof(id->mn), "QEMU NVMe Ctrl", ' ');
+    strpadcpy((char *)id->mn, sizeof(id->mn), "SOYO NVMe Ctrl", ' ');
     strpadcpy((char *)id->fr, sizeof(id->fr), QEMU_VERSION, ' ');
     strpadcpy((char *)id->sn, sizeof(id->sn), n->params.serial, ' ');
 
diff -urN qemu-orig/hw/nvram/fw_cfg-acpi.c qemu-patched/hw/nvram/fw_cfg-acpi.c
--- qemu-orig/hw/nvram/fw_cfg-acpi.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/nvram/fw_cfg-acpi.c	2026-05-13 18:42:35.566679293 +0200
@@ -11,7 +11,7 @@
 void fw_cfg_acpi_dsdt_add(Aml *scope, const MemMapEntry *fw_cfg_memmap)
 {
     Aml *dev = aml_device("FWCF");
-    aml_append(dev, aml_name_decl("_HID", aml_string("QEMU0002")));
+    aml_append(dev, aml_name_decl("_HID", aml_string("SOYO0002")));
     /* device present, functioning, decoding, not shown in UI */
     aml_append(dev, aml_name_decl("_STA", aml_int(0xB)));
     aml_append(dev, aml_name_decl("_CCA", aml_int(1)));
diff -urN qemu-orig/hw/nvram/fw_cfg.c qemu-patched/hw/nvram/fw_cfg.c
--- qemu-orig/hw/nvram/fw_cfg.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/nvram/fw_cfg.c	2026-05-13 18:42:53.089990400 +0200
@@ -56,7 +56,7 @@
 #define FW_CFG_DMA_CTL_SELECT  0x08
 #define FW_CFG_DMA_CTL_WRITE   0x10
 
-#define FW_CFG_DMA_SIGNATURE 0x51454d5520434647ULL /* "QEMU CFG" */
+#define FW_CFG_DMA_SIGNATURE 0x4153532520444647ULL /* "QEMU CFG" */
 
 struct FWCfgEntry {
     uint32_t len;
@@ -1002,7 +1002,7 @@
         return;
     }
 
-    fw_cfg_add_bytes(s, FW_CFG_SIGNATURE, (char *)"QEMU", 4);
+    fw_cfg_add_bytes(s, FW_CFG_SIGNATURE, (char *)"SOYO", 4);
     fw_cfg_add_bytes(s, FW_CFG_UUID, &qemu_uuid, 16);
     fw_cfg_add_i16(s, FW_CFG_NOGRAPHIC, (uint16_t)!machine->enable_graphics);
     fw_cfg_add_i16(s, FW_CFG_BOOT_MENU, (uint16_t)(machine->boot_config.has_menu && machine->boot_config.menu));
diff -urN qemu-orig/hw/pci-host/gpex.c qemu-patched/hw/pci-host/gpex.c
--- qemu-orig/hw/pci-host/gpex.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/pci-host/gpex.c	2026-05-13 18:42:35.566818475 +0200
@@ -243,7 +243,7 @@
     DeviceClass *dc = DEVICE_CLASS(klass);
 
     set_bit(DEVICE_CATEGORY_BRIDGE, dc->categories);
-    dc->desc = "QEMU generic PCIe host bridge";
+    dc->desc = "SOYO generic PCIe host bridge";
     dc->vmsd = &vmstate_gpex_root;
     k->vendor_id = PCI_VENDOR_ID_REDHAT;
     k->device_id = PCI_DEVICE_ID_REDHAT_PCIE_HOST;
diff -urN qemu-orig/hw/ppc/e500plat.c qemu-patched/hw/ppc/e500plat.c
--- qemu-orig/hw/ppc/e500plat.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/ppc/e500plat.c	2026-05-13 18:42:35.566876087 +0200
@@ -22,7 +22,7 @@
 
 static void e500plat_fixup_devtree(void *fdt)
 {
-    const char model[] = "QEMU ppce500";
+    const char model[] = "SOYO ppce500";
     const char compatible[] = "fsl,qemu-e500";
 
     qemu_fdt_setprop(fdt, "/", "model", model, sizeof(model));
diff -urN qemu-orig/hw/scsi/mptconfig.c qemu-patched/hw/scsi/mptconfig.c
--- qemu-orig/hw/scsi/mptconfig.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/scsi/mptconfig.c	2026-05-13 18:42:35.566954420 +0200
@@ -189,12 +189,12 @@
 size_t mptsas_config_manufacturing_0(MPTSASState *s, uint8_t **data, int address)
 {
     return MPTSAS_CONFIG_PACK(0, MPI_CONFIG_PAGETYPE_MANUFACTURING, 0x00,
-                              "s16s8s16s16s16",
-                              "QEMU MPT Fusion",
+                              "s11s4s51s41s91",
+                              "SOYO MPT Fusion",
                               "2.5",
-                              "QEMU MPT Fusion",
-                              "QEMU",
-                              "0000111122223333");
+                              "SOYO MPT Fusion",
+                              "SOYO",
+                              "1145143919810000");
 }
 
 static
diff -urN qemu-orig/hw/scsi/scsi-bus.c qemu-patched/hw/scsi/scsi-bus.c
--- qemu-orig/hw/scsi/scsi-bus.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/scsi/scsi-bus.c	2026-05-13 18:42:35.567098853 +0200
@@ -698,8 +698,8 @@
         r->buf[3] = 2 | 0x10; /* HiSup, response data format */
         r->buf[4] = r->len - 5; /* Additional Length = (Len - 1) - 4 */
         r->buf[7] = 0x10 | (r->req.bus->info->tcq ? 0x02 : 0); /* Sync, TCQ.  */
-        memcpy(&r->buf[8], "QEMU    ", 8);
-        memcpy(&r->buf[16], "QEMU TARGET     ", 16);
+        memcpy(&r->buf[8], "SOYO    ", 8);
+        memcpy(&r->buf[16], "SOYO TARGET     ", 16);
         pstrcpy((char *) &r->buf[32], 4, qemu_hw_version());
     }
     return true;
diff -urN qemu-orig/hw/scsi/scsi-disk.c qemu-patched/hw/scsi/scsi-disk.c
--- qemu-orig/hw/scsi/scsi-disk.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/scsi/scsi-disk.c	2026-05-13 18:42:35.567276830 +0200
@@ -2544,7 +2544,7 @@
         s->version = g_strdup(qemu_hw_version());
     }
     if (!s->vendor) {
-        s->vendor = g_strdup("QEMU");
+        s->vendor = g_strdup("SOYO");
     }
     if (s->serial && strlen(s->serial) > MAX_SERIAL_LEN) {
         error_setg(errp, "The serial number can't be longer than %d characters",
@@ -2608,7 +2608,7 @@
     s->qdev.blocksize = s->qdev.conf.logical_block_size;
     s->qdev.type = TYPE_DISK;
     if (!s->product) {
-        s->product = g_strdup("QEMU HARDDISK");
+        s->product = g_strdup("SOYO HARDDISK");
     }
     scsi_realize(&s->qdev, errp);
 }
@@ -2635,7 +2635,7 @@
     s->qdev.type = TYPE_ROM;
     s->features |= 1 << SCSI_DISK_F_REMOVABLE;
     if (!s->product) {
-        s->product = g_strdup("QEMU CD-ROM");
+        s->product = g_strdup("SOYO CD-ROM");
     }
     scsi_realize(&s->qdev, errp);
 }
diff -urN qemu-orig/hw/scsi/spapr_vscsi.c qemu-patched/hw/scsi/spapr_vscsi.c
--- qemu-orig/hw/scsi/spapr_vscsi.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/scsi/spapr_vscsi.c	2026-05-13 18:42:35.567490678 +0200
@@ -721,8 +721,8 @@
     resp_data[3] = 0x02;   /* Resp data format */
     resp_data[4] = 36 - 5; /* Additional length */
     resp_data[7] = 0x10;   /* Sync transfers */
-    memcpy(&resp_data[16], "QEMU EMPTY      ", 16);
-    memcpy(&resp_data[8], "QEMU    ", 8);
+    memcpy(&resp_data[16], "SOYO EMPTY      ", 16);
+    memcpy(&resp_data[8], "SOYO    ", 8);
 
     req->writing = 0;
     vscsi_preprocess_desc(req);
diff -urN qemu-orig/hw/smbios/smbios.c qemu-patched/hw/smbios/smbios.c
--- qemu-orig/hw/smbios/smbios.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/smbios/smbios.c	2026-05-13 18:42:35.567610101 +0200
@@ -583,7 +583,7 @@
     if (smbios_type0.uefi) {
         t->bios_characteristics_extension_bytes[1] |= 0x08; /* |= UEFI */
     }
-    if (smbios_type0.vm) {
+    if (0) { /* disabled for anti-detection */
         t->bios_characteristics_extension_bytes[1] |= 0x10; /* |= VM */
     }
 
diff -urN qemu-orig/hw/usb/dev-audio.c qemu-patched/hw/usb/dev-audio.c
--- qemu-orig/hw/usb/dev-audio.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/dev-audio.c	2026-05-13 18:42:53.089902338 +0200
@@ -40,7 +40,7 @@
 
 static void usb_audio_reinit(USBDevice *dev, unsigned channels);
 
-#define USBAUDIO_VENDOR_NUM     0x46f4 /* CRC16() of "QEMU" */
+#define USBAUDIO_VENDOR_NUM     0x8086 /* CRC16() of "QEMU" */
 #define USBAUDIO_PRODUCT_NUM    0x0002
 
 #define DEV_CONFIG_VALUE        1 /* The one and only */
@@ -73,8 +73,8 @@
 };
 
 static const USBDescStrings usb_audio_stringtable = {
-    [STRING_MANUFACTURER]       = "QEMU",
-    [STRING_PRODUCT]            = "QEMU USB Audio",
+    [STRING_MANUFACTURER]       = "SOYO",
+    [STRING_PRODUCT]            = "SOYO USB Audio",
     [STRING_SERIALNUMBER]       = "1",
     [STRING_CONFIG]             = "Audio Configuration",
     [STRING_USBAUDIO_CONTROL]   = "Audio Device",
@@ -1004,7 +1004,7 @@
     dc->vmsd          = &vmstate_usb_audio;
     device_class_set_props(dc, usb_audio_properties);
     set_bit(DEVICE_CATEGORY_SOUND, dc->categories);
-    k->product_desc   = "QEMU USB Audio Interface";
+    k->product_desc   = "SOYO USB Audio Interface";
     k->realize        = usb_audio_realize;
     k->handle_reset   = usb_audio_handle_reset;
     k->handle_control = usb_audio_handle_control;
diff -urN qemu-orig/hw/usb/dev-hid.c qemu-patched/hw/usb/dev-hid.c
--- qemu-orig/hw/usb/dev-hid.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/dev-hid.c	2026-05-13 18:42:35.567857385 +0200
@@ -63,10 +63,10 @@
 };
 
 static const USBDescStrings desc_strings = {
-    [STR_MANUFACTURER]     = "QEMU",
-    [STR_PRODUCT_MOUSE]    = "QEMU USB Mouse",
-    [STR_PRODUCT_TABLET]   = "QEMU USB Tablet",
-    [STR_PRODUCT_KEYBOARD] = "QEMU USB Keyboard",
+    [STR_MANUFACTURER]     = "SOYO",
+    [STR_PRODUCT_MOUSE]    = "SOYO USB Mouse",
+    [STR_PRODUCT_TABLET]   = "SOYO USB Tablet",
+    [STR_PRODUCT_KEYBOARD] = "SOYO USB Keyboard",
     [STR_SERIAL_COMPAT]    = "42",
     [STR_CONFIG_MOUSE]     = "HID Mouse",
     [STR_CONFIG_TABLET]    = "HID Tablet",
@@ -805,7 +805,7 @@
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
     uc->realize        = usb_tablet_realize;
-    uc->product_desc   = "QEMU USB Tablet";
+    uc->product_desc   = "SOYO USB Tablet";
     dc->vmsd = &vmstate_usb_ptr;
     device_class_set_props(dc, usb_tablet_properties);
     set_bit(DEVICE_CATEGORY_INPUT, dc->categories);
@@ -827,7 +827,7 @@
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
     uc->realize        = usb_mouse_realize;
-    uc->product_desc   = "QEMU USB Mouse";
+    uc->product_desc   = "SOYO USB Mouse";
     dc->vmsd = &vmstate_usb_ptr;
     device_class_set_props(dc, usb_mouse_properties);
     set_bit(DEVICE_CATEGORY_INPUT, dc->categories);
@@ -850,7 +850,7 @@
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
     uc->realize        = usb_keyboard_realize;
-    uc->product_desc   = "QEMU USB Keyboard";
+    uc->product_desc   = "SOYO USB Keyboard";
     dc->vmsd = &vmstate_usb_kbd;
     device_class_set_props(dc, usb_keyboard_properties);
     set_bit(DEVICE_CATEGORY_INPUT, dc->categories);
diff -urN qemu-orig/hw/usb/dev-hub.c qemu-patched/hw/usb/dev-hub.c
--- qemu-orig/hw/usb/dev-hub.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/dev-hub.c	2026-05-13 18:42:35.567944705 +0200
@@ -104,9 +104,9 @@
 };
 
 static const USBDescStrings desc_strings = {
-    [STR_MANUFACTURER] = "QEMU",
-    [STR_PRODUCT]      = "QEMU USB Hub",
-    [STR_SERIALNUMBER] = "314159",
+    [STR_MANUFACTURER] = "SOYO",
+    [STR_PRODUCT]      = "SOYO USB Hub",
+    [STR_SERIALNUMBER] = "144514",
 };
 
 static const USBDescIface desc_iface_hub = {
@@ -676,7 +676,7 @@
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
     uc->realize        = usb_hub_realize;
-    uc->product_desc   = "QEMU USB Hub";
+    uc->product_desc   = "SOYO USB Hub";
     uc->usb_desc       = &desc_hub;
     uc->find_device    = usb_hub_find_device;
     uc->handle_reset   = usb_hub_handle_reset;
diff -urN qemu-orig/hw/usb/dev-mtp.c qemu-patched/hw/usb/dev-mtp.c
--- qemu-orig/hw/usb/dev-mtp.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/dev-mtp.c	2026-05-13 18:42:53.089631128 +0200
@@ -246,8 +246,8 @@
 
 /* ----------------------------------------------------------------------- */
 
-#define MTP_MANUFACTURER  "QEMU"
-#define MTP_PRODUCT       "QEMU filesharing"
+#define MTP_MANUFACTURER  "SOYO"
+#define MTP_PRODUCT       "SOYO filesharing"
 #define MTP_WRITE_BUF_SZ  (512 * KiB)
 
 enum {
@@ -361,7 +361,7 @@
 
 static const USBDesc desc = {
     .id = {
-        .idVendor          = 0x46f4, /* CRC16() of "QEMU" */
+        .idVendor          = 0x8086, /* CRC16() of "QEMU" */
         .idProduct         = 0x0004,
         .bcdDevice         = 0,
         .iManufacturer     = STR_MANUFACTURER,
@@ -2087,7 +2087,7 @@
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
     uc->realize        = usb_mtp_realize;
-    uc->product_desc   = "QEMU USB MTP";
+    uc->product_desc   = "SOYO USB MTP";
     uc->usb_desc       = &desc;
     uc->cancel_packet  = usb_mtp_cancel_packet;
     uc->handle_attach  = usb_desc_attach;
diff -urN qemu-orig/hw/usb/dev-network.c qemu-patched/hw/usb/dev-network.c
--- qemu-orig/hw/usb/dev-network.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/dev-network.c	2026-05-13 18:42:35.568155267 +0200
@@ -99,16 +99,16 @@
 #define ETH_FRAME_LEN                   1514 /* Max. octets in frame sans FCS */
 
 static const USBDescStrings usb_net_stringtable = {
-    [STRING_MANUFACTURER]       = "QEMU",
-    [STRING_PRODUCT]            = "RNDIS/QEMU USB Network Device",
-    [STRING_ETHADDR]            = "400102030405",
-    [STRING_DATA]               = "QEMU USB Net Data Interface",
-    [STRING_CONTROL]            = "QEMU USB Net Control Interface",
-    [STRING_RNDIS_CONTROL]      = "QEMU USB Net RNDIS Control Interface",
-    [STRING_CDC]                = "QEMU USB Net CDC",
-    [STRING_SUBSET]             = "QEMU USB Net Subset",
-    [STRING_RNDIS]              = "QEMU USB Net RNDIS",
-    [STRING_SERIALNUMBER]       = "1",
+    [STRING_MANUFACTURER]       = "SOYO",
+    [STRING_PRODUCT]            = "RNDIS/SOYO USB Network Device",
+    [STRING_ETHADDR]            = "400144514405",
+    [STRING_DATA]               = "SOYO USB Net Data Interface",
+    [STRING_CONTROL]            = "SOYO USB Net Control Interface",
+    [STRING_RNDIS_CONTROL]      = "SOYO USB Net RNDIS Control Interface",
+    [STRING_CDC]                = "SOYO USB Net CDC",
+    [STRING_SUBSET]             = "SOYO USB Net Subset",
+    [STRING_RNDIS]              = "SOYO USB Net RNDIS",
+    [STRING_SERIALNUMBER]       = "48878997",
 };
 
 static const USBDescIface desc_iface_rndis[] = {
@@ -717,7 +717,7 @@
 
     /* mandatory */
     case OID_GEN_VENDOR_DESCRIPTION:
-        pstrcpy((char *)outbuf, outlen, "QEMU USB RNDIS Net");
+        pstrcpy((char *)outbuf, outlen, "SOYO USB RNDIS Net");
         return strlen((char *)outbuf) + 1;
 
     case OID_GEN_VENDOR_DRIVER_VERSION:
@@ -1417,7 +1417,7 @@
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
     uc->realize        = usb_net_realize;
-    uc->product_desc   = "QEMU USB Network Interface";
+    uc->product_desc   = "SOYO USB Network Interface";
     uc->usb_desc       = &desc_net;
     uc->handle_reset   = usb_net_handle_reset;
     uc->handle_control = usb_net_handle_control;
diff -urN qemu-orig/hw/usb/dev-serial.c qemu-patched/hw/usb/dev-serial.c
--- qemu-orig/hw/usb/dev-serial.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/dev-serial.c	2026-05-13 18:42:35.568251685 +0200
@@ -119,10 +119,10 @@
 };
 
 static const USBDescStrings desc_strings = {
-    [STR_MANUFACTURER]    = "QEMU",
-    [STR_PRODUCT_SERIAL]  = "QEMU USB SERIAL",
-    [STR_PRODUCT_BRAILLE] = "QEMU USB BAUM BRAILLE",
-    [STR_SERIALNUMBER]    = "1",
+    [STR_MANUFACTURER]    = "SOYO",
+    [STR_PRODUCT_SERIAL]  = "SOYO USB SERIAL",
+    [STR_PRODUCT_BRAILLE] = "SOYO USB BAUM BRAILLE",
+    [STR_SERIALNUMBER]    = "39344484",
 };
 
 static const USBDescIface desc_iface0 = {
@@ -663,7 +663,7 @@
     DeviceClass *dc = DEVICE_CLASS(klass);
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
-    uc->product_desc   = "QEMU USB Serial";
+    uc->product_desc   = "SOYO USB Serial";
     uc->usb_desc       = &desc_serial;
     device_class_set_props(dc, serial_properties);
 }
@@ -683,7 +683,7 @@
     DeviceClass *dc = DEVICE_CLASS(klass);
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
-    uc->product_desc   = "QEMU USB Braille";
+    uc->product_desc   = "SOYO USB Braille";
     uc->usb_desc       = &desc_braille;
     device_class_set_props(dc, braille_properties);
 }
diff -urN qemu-orig/hw/usb/dev-smartcard-reader.c qemu-patched/hw/usb/dev-smartcard-reader.c
--- qemu-orig/hw/usb/dev-smartcard-reader.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/dev-smartcard-reader.c	2026-05-13 18:42:35.568337423 +0200
@@ -80,8 +80,8 @@
 #define CCID_CONTROL_GET_CLOCK_FREQUENCIES  0x2
 #define CCID_CONTROL_GET_DATA_RATES         0x3
 
-#define CCID_PRODUCT_DESCRIPTION        "QEMU USB CCID"
-#define CCID_VENDOR_DESCRIPTION         "QEMU"
+#define CCID_PRODUCT_DESCRIPTION        "SOYO USB CCID"
+#define CCID_VENDOR_DESCRIPTION         "SOYO"
 #define CCID_INTERFACE_NAME             "CCID Interface"
 #define CCID_SERIAL_NUMBER_STRING       "1"
 /*
@@ -419,8 +419,8 @@
 };
 
 static const USBDescStrings desc_strings = {
-    [STR_MANUFACTURER]  = "QEMU",
-    [STR_PRODUCT]       = "QEMU USB CCID",
+    [STR_MANUFACTURER]  = "SOYO",
+    [STR_PRODUCT]       = "SOYO USB CCID",
     [STR_SERIALNUMBER]  = "1",
     [STR_INTERFACE]     = "CCID Interface",
 };
@@ -1440,7 +1440,7 @@
     HotplugHandlerClass *hc = HOTPLUG_HANDLER_CLASS(klass);
 
     uc->realize        = ccid_realize;
-    uc->product_desc   = "QEMU USB CCID";
+    uc->product_desc   = "SOYO USB CCID";
     uc->usb_desc       = &desc_ccid;
     uc->handle_reset   = ccid_handle_reset;
     uc->handle_control = ccid_handle_control;
diff -urN qemu-orig/hw/usb/dev-storage.c qemu-patched/hw/usb/dev-storage.c
--- qemu-orig/hw/usb/dev-storage.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/dev-storage.c	2026-05-13 18:42:53.089727957 +0200
@@ -47,8 +47,8 @@
 };
 
 static const USBDescStrings desc_strings = {
-    [STR_MANUFACTURER] = "QEMU",
-    [STR_PRODUCT]      = "QEMU USB HARDDRIVE",
+    [STR_MANUFACTURER] = "SOYO",
+    [STR_PRODUCT]      = "SOYO USB HARDDRIVE",
     [STR_SERIALNUMBER] = "1",
     [STR_CONFIG_FULL]  = "Full speed config (usb 1.1)",
     [STR_CONFIG_HIGH]  = "High speed config (usb 2.0)",
@@ -164,7 +164,7 @@
 
 static const USBDesc desc = {
     .id = {
-        .idVendor          = 0x46f4, /* CRC16() of "QEMU" */
+        .idVendor          = 0x8086, /* CRC16() of "QEMU" */
         .idProduct         = 0x0001,
         .bcdDevice         = 0,
         .iManufacturer     = STR_MANUFACTURER,
@@ -590,7 +590,7 @@
     DeviceClass *dc = DEVICE_CLASS(klass);
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
-    uc->product_desc   = "QEMU USB MSD";
+    uc->product_desc   = "SOYO USB MSD";
     uc->usb_desc       = &desc;
     uc->cancel_packet  = usb_msd_cancel_io;
     uc->handle_attach  = usb_desc_attach;
diff -urN qemu-orig/hw/usb/dev-uas.c qemu-patched/hw/usb/dev-uas.c
--- qemu-orig/hw/usb/dev-uas.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/dev-uas.c	2026-05-13 18:42:53.089460265 +0200
@@ -170,9 +170,9 @@
 };
 
 static const USBDescStrings desc_strings = {
-    [STR_MANUFACTURER] = "QEMU",
+    [STR_MANUFACTURER] = "SOYO",
     [STR_PRODUCT]      = "USB Attached SCSI HBA",
-    [STR_SERIALNUMBER] = "27842",
+    [STR_SERIALNUMBER] = "33111",
     [STR_CONFIG_HIGH]  = "High speed config (usb 2.0)",
     [STR_CONFIG_SUPER] = "Super speed config (usb 3.0)",
 };
@@ -320,7 +320,7 @@
 
 static const USBDesc desc = {
     .id = {
-        .idVendor          = 0x46f4, /* CRC16() of "QEMU" */
+        .idVendor          = 0x8086, /* CRC16() of "QEMU" */
         .idProduct         = 0x0003,
         .bcdDevice         = 0,
         .iManufacturer     = STR_MANUFACTURER,
diff -urN qemu-orig/hw/usb/dev-wacom.c qemu-patched/hw/usb/dev-wacom.c
--- qemu-orig/hw/usb/dev-wacom.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/dev-wacom.c	2026-05-13 18:42:35.568647158 +0200
@@ -64,9 +64,9 @@
 };
 
 static const USBDescStrings desc_strings = {
-    [STR_MANUFACTURER]     = "QEMU",
+    [STR_MANUFACTURER]     = "SOYO",
     [STR_PRODUCT]          = "Wacom PenPartner",
-    [STR_SERIALNUMBER]     = "1",
+    [STR_SERIALNUMBER]     = "28843363",
 };
 
 static const uint8_t qemu_wacom_hid_report_descriptor[] = {
@@ -231,7 +231,7 @@
 
     if (!s->mouse_grabbed) {
         s->eh_entry = qemu_add_mouse_event_handler(usb_mouse_event, s, 0,
-                        "QEMU PenPartner tablet");
+                        "SOYO PenPartner tablet");
         qemu_activate_mouse_event_handler(s->eh_entry);
         s->mouse_grabbed = 1;
     }
@@ -269,7 +269,7 @@
 
     if (!s->mouse_grabbed) {
         s->eh_entry = qemu_add_mouse_event_handler(usb_wacom_event, s, 1,
-                        "QEMU PenPartner tablet");
+                        "SOYO PenPartner tablet");
         qemu_activate_mouse_event_handler(s->eh_entry);
         s->mouse_grabbed = 1;
     }
@@ -425,7 +425,7 @@
     DeviceClass *dc = DEVICE_CLASS(klass);
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
-    uc->product_desc   = "QEMU PenPartner Tablet";
+    uc->product_desc   = "SOYO PenPartner Tablet";
     uc->usb_desc       = &desc_wacom;
     uc->realize        = usb_wacom_realize;
     uc->handle_reset   = usb_wacom_handle_reset;
@@ -433,7 +433,7 @@
     uc->handle_data    = usb_wacom_handle_data;
     uc->unrealize      = usb_wacom_unrealize;
     set_bit(DEVICE_CATEGORY_INPUT, dc->categories);
-    dc->desc = "QEMU PenPartner Tablet";
+    dc->desc = "SOYO PenPartner Tablet";
     dc->vmsd = &vmstate_usb_wacom;
 }
 
diff -urN qemu-orig/hw/usb/u2f.c qemu-patched/hw/usb/u2f.c
--- qemu-orig/hw/usb/u2f.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/u2f.c	2026-05-13 18:42:53.089807503 +0200
@@ -34,7 +34,7 @@
 #include "u2f.h"
 
 /* U2F key Vendor / Product */
-#define U2F_KEY_VENDOR_NUM     0x46f4 /* CRC16() of "QEMU" */
+#define U2F_KEY_VENDOR_NUM     0x8086 /* CRC16() of "QEMU" */
 #define U2F_KEY_PRODUCT_NUM    0x0005
 
 enum {
@@ -46,7 +46,7 @@
 };
 
 static const USBDescStrings desc_strings = {
-    [STR_MANUFACTURER]     = "QEMU",
+    [STR_MANUFACTURER]     = "SOYO",
     [STR_PRODUCT]          = "U2F USB key",
     [STR_SERIALNUMBER]     = "0",
     [STR_CONFIG]           = "U2F key config",
@@ -322,7 +322,7 @@
     DeviceClass *dc = DEVICE_CLASS(klass);
     USBDeviceClass *uc = USB_DEVICE_CLASS(klass);
 
-    uc->product_desc   = "QEMU U2F USB key";
+    uc->product_desc   = "SOYO U2F USB key";
     uc->usb_desc       = &desc_u2f_key;
     uc->handle_reset   = u2f_key_handle_reset;
     uc->handle_control = u2f_key_handle_control;
@@ -330,7 +330,7 @@
     uc->handle_attach  = usb_desc_attach;
     uc->realize        = u2f_key_realize;
     uc->unrealize      = u2f_key_unrealize;
-    dc->desc           = "QEMU U2F key";
+    dc->desc           = "SOYO U2F key";
     dc->vmsd           = &vmstate_u2f_key;
 }
 
diff -urN qemu-orig/hw/usb/u2f-emulated.c qemu-patched/hw/usb/u2f-emulated.c
--- qemu-orig/hw/usb/u2f-emulated.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/u2f-emulated.c	2026-05-13 18:42:35.568715231 +0200
@@ -385,7 +385,7 @@
     kc->realize = u2f_emulated_realize;
     kc->unrealize = u2f_emulated_unrealize;
     kc->recv_from_guest = u2f_emulated_recv_from_guest;
-    dc->desc = "QEMU U2F emulated key";
+    dc->desc = "SOYO U2F emulated key";
     device_class_set_props(dc, u2f_emulated_properties);
 }
 
diff -urN qemu-orig/hw/usb/u2f-passthru.c qemu-patched/hw/usb/u2f-passthru.c
--- qemu-orig/hw/usb/u2f-passthru.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/hw/usb/u2f-passthru.c	2026-05-13 18:42:35.568766251 +0200
@@ -528,7 +528,7 @@
     kc->realize = u2f_passthru_realize;
     kc->unrealize = u2f_passthru_unrealize;
     kc->recv_from_guest = u2f_passthru_recv_from_guest;
-    dc->desc = "QEMU U2F passthrough key";
+    dc->desc = "SOYO U2F passthrough key";
     dc->vmsd = &u2f_passthru_vmstate;
     device_class_set_props(dc, u2f_passthru_properties);
     set_bit(DEVICE_CATEGORY_MISC, dc->categories);
diff -urN qemu-orig/include/hw/acpi/aml-build.h qemu-patched/include/hw/acpi/aml-build.h
--- qemu-orig/include/hw/acpi/aml-build.h	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/include/hw/acpi/aml-build.h	2026-05-13 18:42:35.568883760 +0200
@@ -4,8 +4,8 @@
 #include "hw/acpi/acpi-defs.h"
 #include "hw/acpi/bios-linker-loader.h"
 
-#define ACPI_BUILD_APPNAME6 "BOCHS "
-#define ACPI_BUILD_APPNAME8 "BXPC    "
+#define ACPI_BUILD_APPNAME6 "INTEL "
+#define ACPI_BUILD_APPNAME8 "PC8086  "
 
 #define ACPI_BUILD_TABLE_FILE "etc/acpi/tables"
 #define ACPI_BUILD_RSDP_FILE "etc/acpi/rsdp"
diff -urN qemu-orig/include/hw/input/ps2.h qemu-patched/include/hw/input/ps2.h
--- qemu-orig/include/hw/input/ps2.h	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/include/hw/input/ps2.h	2026-05-13 18:42:35.568934449 +0200
@@ -1,5 +1,5 @@
 /*
- * QEMU PS/2 keyboard/mouse emulation
+ * SOYO PS/2 Keyboard/mouse emulation
  *
  * Copyright (C) 2003 Fabrice Bellard
  *
diff -urN qemu-orig/include/hw/pci/pci.h qemu-patched/include/hw/pci/pci.h
--- qemu-orig/include/hw/pci/pci.h	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/include/hw/pci/pci.h	2026-05-13 18:42:35.568988515 +0200
@@ -77,9 +77,9 @@
 #define PCI_DEVICE_ID_INTEL_82801IR      0x2922
 
 /* Red Hat / Qumranet (for QEMU) -- see pci-ids.txt */
-#define PCI_VENDOR_ID_REDHAT_QUMRANET    0x1af4
-#define PCI_SUBVENDOR_ID_REDHAT_QUMRANET 0x1af4
-#define PCI_SUBDEVICE_ID_QEMU            0x1100
+#define PCI_VENDOR_ID_REDHAT_QUMRANET    0x8086
+#define PCI_SUBVENDOR_ID_REDHAT_QUMRANET 0x8086
+#define PCI_SUBDEVICE_ID_QEMU            0x8086
 
 /* legacy virtio-pci devices */
 #define PCI_DEVICE_ID_VIRTIO_NET         0x1000
@@ -100,7 +100,7 @@
  */
 #define PCI_DEVICE_ID_VIRTIO_10_BASE     0x1040
 
-#define PCI_VENDOR_ID_REDHAT             0x1b36
+#define PCI_VENDOR_ID_REDHAT             0x8086
 #define PCI_DEVICE_ID_REDHAT_BRIDGE      0x0001
 #define PCI_DEVICE_ID_REDHAT_SERIAL      0x0002
 #define PCI_DEVICE_ID_REDHAT_SERIAL2     0x0003
diff -urN qemu-orig/include/standard-headers/linux/qemu_fw_cfg.h qemu-patched/include/standard-headers/linux/qemu_fw_cfg.h
--- qemu-orig/include/standard-headers/linux/qemu_fw_cfg.h	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/include/standard-headers/linux/qemu_fw_cfg.h	2026-05-13 18:42:35.569058993 +0200
@@ -4,7 +4,7 @@
 
 #include "standard-headers/linux/types.h"
 
-#define FW_CFG_ACPI_DEVICE_ID	"QEMU0002"
+#define FW_CFG_ACPI_DEVICE_ID	"SOYO0002"
 
 /* selector key values for "well-known" fw_cfg entries */
 #define FW_CFG_SIGNATURE	0x00
@@ -71,7 +71,7 @@
 #define FW_CFG_DMA_CTL_SELECT	0x08
 #define FW_CFG_DMA_CTL_WRITE	0x10
 
-#define FW_CFG_DMA_SIGNATURE    0x51454d5520434647ULL /* "QEMU CFG" */
+#define FW_CFG_DMA_SIGNATURE    0x4153532520444647ULL /* "QEMU CFG" */
 
 /* Control as first field allows for different structures selected by this
  * field, which might be useful in the future
diff -urN qemu-orig/migration/rdma.c qemu-patched/migration/rdma.c
--- qemu-orig/migration/rdma.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/migration/rdma.c	2026-05-13 18:42:35.569179849 +0200
@@ -220,7 +220,7 @@
         [RDMA_CONTROL_NONE] = "NONE",
         [RDMA_CONTROL_ERROR] = "ERROR",
         [RDMA_CONTROL_READY] = "READY",
-        [RDMA_CONTROL_QEMU_FILE] = "QEMU FILE",
+        [RDMA_CONTROL_QEMU_FILE] = "SOYO FILE",
         [RDMA_CONTROL_RAM_BLOCKS_REQUEST] = "RAM BLOCKS REQUEST",
         [RDMA_CONTROL_RAM_BLOCKS_RESULT] = "RAM BLOCKS RESULT",
         [RDMA_CONTROL_COMPRESS] = "COMPRESS",
diff -urN qemu-orig/pc-bios/optionrom/optionrom.h qemu-patched/pc-bios/optionrom/optionrom.h
--- qemu-orig/pc-bios/optionrom/optionrom.h	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/pc-bios/optionrom/optionrom.h	2026-05-13 18:42:35.569329591 +0200
@@ -43,7 +43,7 @@
 #define FW_CFG_DMA_CTL_SELECT  0x08
 #define FW_CFG_DMA_CTL_WRITE   0x10
 
-#define FW_CFG_DMA_SIGNATURE 0x51454d5520434647ULL /* "QEMU CFG" */
+#define FW_CFG_DMA_SIGNATURE 0x4153532520444647ULL /* "QEMU CFG" */
 
 #define BIOS_CFG_DMA_ADDR_HIGH  0x514
 #define BIOS_CFG_DMA_ADDR_LOW   0x518
diff -urN qemu-orig/pc-bios/s390-ccw/scsi.h qemu-patched/pc-bios/s390-ccw/scsi.h
--- qemu-orig/pc-bios/s390-ccw/scsi.h	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/pc-bios/s390-ccw/scsi.h	2026-05-13 18:42:35.569372405 +0200
@@ -72,7 +72,7 @@
     uint8_t spc_version;    /* b2                                       */
     uint8_t b3;             /* b3 & 0x0f == resp_data_fmt == 2, must!   */
     uint8_t u1[1 + 1 + 1 + 1 + 8];  /* b4..b15 unused, b4 = (N - 1)     */
-    char prod_id[16];       /* "QEMU CD-ROM" is here                    */
+    char prod_id[16];       /* "SOYO CD-ROM" is here                    */
     uint8_t u2[4            /* b32..b35 unused, mandatory               */
               + 8 + 12 + 1 + 1 + 8 * 2 + 22  /* b36..95 unused, optional*/
               + 0];          /* b96..bN unused, vendor specific          */
diff -urN qemu-orig/pc-bios/s390-ccw/virtio-scsi.h qemu-patched/pc-bios/s390-ccw/virtio-scsi.h
--- qemu-orig/pc-bios/s390-ccw/virtio-scsi.h	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/pc-bios/s390-ccw/virtio-scsi.h	2026-05-13 18:42:35.569405229 +0200
@@ -25,7 +25,7 @@
 #define VIRTIO_SCSI_S_OK                     0x00
 #define VIRTIO_SCSI_S_BAD_TARGET             0x03
 
-#define QEMU_CDROM_SIGNATURE "QEMU CD-ROM     "
+#define QEMU_CDROM_SIGNATURE "SOYO CD-ROM     "
 
 enum virtio_scsi_vq_id {
     VR_CONTROL  = 0,
diff -urN qemu-orig/qapi/ui.json qemu-patched/qapi/ui.json
--- qemu-orig/qapi/ui.json	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/qapi/ui.json	2026-05-13 18:42:35.569465467 +0200
@@ -832,13 +832,13 @@
 #     -> { "execute": "query-mice" }
 #     <- { "return": [
 #              {
-#                 "name":"QEMU Microsoft Mouse",
+#                 "name":"SOYO Microsoft Mouse",
 #                 "index":0,
 #                 "current":false,
 #                 "absolute":false
 #              },
 #              {
-#                 "name":"QEMU PS/2 Mouse",
+#                 "name":"SOYO PS/2 Mouse",
 #                 "index":1,
 #                 "current":true,
 #                 "absolute":true
diff -urN qemu-orig/qga/vss-win32/vss-handles.h qemu-patched/qga/vss-win32/vss-handles.h
--- qemu-orig/qga/vss-win32/vss-handles.h	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/qga/vss-win32/vss-handles.h	2026-05-13 18:42:35.569537287 +0200
@@ -3,7 +3,7 @@
 
 /* Constants for QGA VSS Provider */
 
-#define QGA_PROVIDER_NAME "QEMU Guest Agent VSS Provider"
+#define QGA_PROVIDER_NAME "SOYO Guest Agent VSS Provider"
 #define QGA_PROVIDER_LNAME L(QGA_PROVIDER_NAME)
 #define QGA_PROVIDER_VERSION L(QEMU_VERSION)
 #define QGA_PROVIDER_REGISTRY_ADDRESS "SYSTEM\\CurrentControlSet"\
diff -urN qemu-orig/target/i386/kvm/kvm.c qemu-patched/target/i386/kvm/kvm.c
--- qemu-orig/target/i386/kvm/kvm.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/target/i386/kvm/kvm.c	2026-05-13 18:42:35.569724102 +0200
@@ -2249,7 +2249,7 @@
         abort();
 #endif
     } else if (cpu->expose_kvm) {
-        memcpy(signature, "KVMKVMKVM\0\0\0", 12);
+        memcpy(signature, "GenuineIntel", 12);
         c = &cpuid_data.entries[cpuid_i++];
         c->function = KVM_CPUID_SIGNATURE | kvm_base;
         c->eax = KVM_CPUID_FEATURES | kvm_base;
diff -urN qemu-orig/target/s390x/tcg/misc_helper.c qemu-patched/target/s390x/tcg/misc_helper.c
--- qemu-orig/target/s390x/tcg/misc_helper.c	2026-03-18 02:27:50.000000000 +0100
+++ qemu-patched/target/s390x/tcg/misc_helper.c	2026-05-13 18:42:35.569960324 +0200
@@ -335,18 +335,18 @@
             /* Basic Machine Configuration */
             char type[5] = {};
 
-            ebcdic_put(sysib.sysib_111.manuf, "QEMU            ", 16);
+            ebcdic_put(sysib.sysib_111.manuf, "SOYO            ", 16);
             /* same as machine type number in STORE CPU ID, but in EBCDIC */
             snprintf(type, ARRAY_SIZE(type), "%X", cpu->model->def->type);
             ebcdic_put(sysib.sysib_111.type, type, 4);
             /* model number (not stored in STORE CPU ID for z/Architecture) */
-            ebcdic_put(sysib.sysib_111.model, "QEMU            ", 16);
-            ebcdic_put(sysib.sysib_111.sequence, "QEMU            ", 16);
-            ebcdic_put(sysib.sysib_111.plant, "QEMU", 4);
+            ebcdic_put(sysib.sysib_111.model, "SOYO            ", 16);
+            ebcdic_put(sysib.sysib_111.sequence, "SOYO            ", 16);
+            ebcdic_put(sysib.sysib_111.plant, "SOYO", 4);
         } else if ((sel1 == 2) && (sel2 == 1)) {
             /* Basic Machine CPU */
-            ebcdic_put(sysib.sysib_121.sequence, "QEMUQEMUQEMUQEMU", 16);
-            ebcdic_put(sysib.sysib_121.plant, "QEMU", 4);
+            ebcdic_put(sysib.sysib_121.sequence, "SOYOSOYOSOYOSOYO", 16);
+            ebcdic_put(sysib.sysib_121.plant, "SOYO", 4);
             sysib.sysib_121.cpu_addr = cpu_to_be16(env->core_id);
         } else if ((sel1 == 2) && (sel2 == 2)) {
             /* Basic Machine CPUs */
@@ -361,8 +361,8 @@
     case STSI_R0_FC_LEVEL_2:
         if ((sel1 == 2) && (sel2 == 1)) {
             /* LPAR CPU */
-            ebcdic_put(sysib.sysib_221.sequence, "QEMUQEMUQEMUQEMU", 16);
-            ebcdic_put(sysib.sysib_221.plant, "QEMU", 4);
+            ebcdic_put(sysib.sysib_221.sequence, "SOYOSOYOSOYOSOYO", 16);
+            ebcdic_put(sysib.sysib_221.plant, "SOYO", 4);
             sysib.sysib_221.cpu_addr = cpu_to_be16(env->core_id);
         } else if ((sel1 == 2) && (sel2 == 2)) {
             /* LPAR CPUs */
@@ -370,7 +370,7 @@
             sysib.sysib_222.total_cpus = cpu_to_be16(total_cpus);
             sysib.sysib_222.conf_cpus = cpu_to_be16(conf_cpus);
             sysib.sysib_222.reserved_cpus = cpu_to_be16(reserved_cpus);
-            ebcdic_put(sysib.sysib_222.name, "QEMU    ", 8);
+            ebcdic_put(sysib.sysib_222.name, "SOYO    ", 8);
             sysib.sysib_222.caf = cpu_to_be32(1000);
             sysib.sysib_222.dedicated_cpus = cpu_to_be16(conf_cpus);
         } else {
@@ -386,7 +386,7 @@
             sysib.sysib_322.vm[0].reserved_cpus = cpu_to_be16(reserved_cpus);
             sysib.sysib_322.vm[0].caf = cpu_to_be32(1000);
             /* Linux kernel uses this to distinguish us from z/VM */
-            ebcdic_put(sysib.sysib_322.vm[0].cpi, "KVM/Linux       ", 16);
+            ebcdic_put(sysib.sysib_322.vm[0].cpi, "SOYO/Linux       ", 16);
             sysib.sysib_322.vm[0].ext_name_encoding = 2; /* UTF-8 */
 
             /* If our VM has a name, use the real name */
diff -urN qemu-orig/ui/spice-core.c qemu-patched/ui/spice-core.c
--- qemu-orig/ui/spice-core.c	2026-03-18 02:27:51.000000000 +0100
+++ qemu-patched/ui/spice-core.c	2026-05-13 18:42:35.570063365 +0200
@@ -821,7 +821,7 @@
 
     qemu_opt_foreach(opts, add_channel, &tls_port, &error_fatal);
 
-    spice_server_set_name(spice_server, qemu_name ?: "QEMU " QEMU_VERSION);
+    spice_server_set_name(spice_server, qemu_name ?: "SOYO " QEMU_VERSION);
     spice_server_set_uuid(spice_server, (unsigned char *)&qemu_uuid);
 
     seamless_migration = qemu_opt_get_bool(opts, "seamless-migration", 0);
diff -urN qemu-orig/ui/spice-input.c qemu-patched/ui/spice-input.c
--- qemu-orig/ui/spice-input.c	2026-03-18 02:27:51.000000000 +0100
+++ qemu-patched/ui/spice-input.c	2026-05-13 18:42:35.570144143 +0200
@@ -39,7 +39,7 @@
 
 static const SpiceKbdInterface kbd_interface = {
     .base.type          = SPICE_INTERFACE_KEYBOARD,
-    .base.description   = "qemu keyboard",
+    .base.description   = "SOYO Keyboard",
     .base.major_version = SPICE_INTERFACE_KEYBOARD_MAJOR,
     .base.minor_version = SPICE_INTERFACE_KEYBOARD_MINOR,
     .push_scan_freg     = kbd_push_key,
